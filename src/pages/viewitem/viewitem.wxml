<!--
  此文件是由模板文件 ".dtpl/page/$rawModuleName.wxml.dtpl" 生成的，你可以自行修改模板

  注意：
  1. 如果没有语法高亮，且你使用的是 vscode 编辑器的话，建义在 vscode 插件库中安装 minapp-vscode 插件
-->

<view>
  <include src="./first.wxml" wx:if="{{nowKey === 'first'}}"></include>
  <include src="./second.wxml" wx:if="{{nowKey === 'second'}}"></include>
  <include src="./fourth.wxml" wx:if="{{nowKey === 'fourth'}}"></include>
</view>
