/******************************************************************
 MIT License http://www.opensource.org/licenses/mit-license.php
 Author Mora <qiuzhongleiabc@126.com> (https://github.com/qiu8310)
*******************************************************************/
export declare type UrlRawSearch = string | {
    [key: string]: string | number | undefined;
};
export declare class Url {
    url: string;
    isTabBar: boolean;
    constructor(url: string, isTabBar?: boolean);
    /**
     * 跳转当前 url 所指定的页面
     *
     * 注意：当页面是 tabBar 中的页面是，不能带参数
     */
    go(searchOrQuery?: UrlRawSearch): void;
    /**
     * 重定向到当前 url 所指定的页面
     *
     * 注意：当页面是 tabBar 时，无法使用此函数
     */
    redirect(searchOrQuery?: UrlRawSearch): void;
    /**
     * 关闭所有页面，打开到应用内的某个页面
     *
     * @since 1.1.0
     */
    reload(searchOrQuery?: UrlRawSearch): void;
}
