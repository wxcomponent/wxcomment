import { Url } from '../feat/Url';
export interface AppifyOptions {
    pages: string[];
    tabBarList: Array<{
        pagePath: string;
        text: string;
    }> | undefined;
}
export declare function appify(options: AppifyOptions, polluteObj?: (obj: any) => void): (SomeApp: new () => BaseApp) => void;
export interface BaseApp extends App, App.BaseOptions {
}
export declare class BaseApp {
    /**
     * 首页的 url，默认是 app.json 中的 pages 中的第一个页面的 Url
     */
    readonly $home: Url;
    readonly $url: {
        [key: string]: Url;
    };
    private __init$home$url(pages?, tabBarList?);
    /**
     * 返回上 1/N 级页面，通过 delta 指定要返回的层级，默认 1
     *
     * @param {number} [delta=1] 要返回的层级
     * @returns
     * @memberof BaseApp
     */
    $back(delta?: number): void;
}
